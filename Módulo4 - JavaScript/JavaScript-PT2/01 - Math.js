/*
     - Math
        - pow(2,2) / potência / 2² = 4.
        - sqrt(25,2) / raiz / √25.
        - pi.
        - ceil / teto -> arredondar para cima.
        - floor / chão -> arredondar para baixo.
        
        - random() / número aleatório entre 0 e 1
 */

// Exemplo usando pow. Onde o primeiro é o numero que quer multiplicar e o sengunda é quatidade de vezes. Ex: 5² = 5x5=25.
const resultPow = Math.pow(5,2)
//console.log(result);

// Exemplo de raiz. Qual número multiplicado por ele mesmo que da 25.
const resultSqrt = Math.sqrt(25,2)
//console.log(resultSqrt);

// Ira mostrar o valor do pi.
const resultPi = Math.PI
//console.log(resultPi);

// Arredonda pra cima, no caso seria 4.
const resultCeil = Math.ceil(3.14)
//console.log(resultCeil);

// Arredondar pra baixo, no caso seria 3.
const resultFloor = Math.floor(3.14)
//console.log(resultFloor);

// Random, será falado na próxima.